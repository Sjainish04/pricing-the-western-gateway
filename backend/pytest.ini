[pytest]
testpaths = tests
markers =
    slow: full Stackelberg searches or multi-equilibrium sweeps. Deselect with
        -m "not slow" for a fast inner loop; CI and any run that reports
        results should include them.
